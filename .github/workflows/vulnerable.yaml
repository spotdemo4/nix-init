name: vulnerable

on:
  schedule:
    - cron: "9 9 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  flake:
    runs-on: ubuntu-latest
    steps:
      - name: Initialize
        uses: spotdemo4/nix-init/.github/actions/init@main
        with:
          shell: ci

      - name: Check flake
        run: flake-checker -f
